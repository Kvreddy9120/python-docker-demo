trigger:
- main

pool:
  name: flm-pool

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- task: CmdLine@2
  inputs:
    script: 'docker build -t myimage .'


- task: CmdLine@2
  inputs:
    script: 'docker run -d --name cont-1 -p 1111:8000 myimage'